---
phase: 21-global-skill-installation-cht-setup-claude-skills
plan: 03
type: execute
wave: 3
depends_on: [21-01, 21-02]
files_modified: []
autonomous: false
requirements: [SETUP-01, SETUP-04]
must_haves:
  truths:
    - "npm pack tarball contains dist/cht.js and all 13 .claude/skills/cht-*/SKILL.md files"
    - "Installing from tarball and running cht setup copies skills and registers hooks"
    - "Full test suite passes"
  artifacts: []
  key_links:
    - from: "package.json files field"
      to: "npm tarball"
      via: "npm pack"
      pattern: "dist/.*\\.claude/skills/"
---

<objective>
Verify the complete build-to-install pipeline works end-to-end: npm pack produces a tarball with the correct contents, global install from tarball yields a working `cht` binary, and `cht setup` copies skills and registers hooks. This is a verification-only plan with no code changes.

Purpose: Catch packaging issues (missing files in tarball, broken import paths) before publishing to npm. This is the gate between "it works locally" and "it works for users".
Output: Verified green status, ready for Phase 20 (documentation + publish).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-global-skill-installation-cht-setup-claude-skills/21-CONTEXT.md
@.planning/phases/21-global-skill-installation-cht-setup-claude-skills/21-01-SUMMARY.md
@.planning/phases/21-global-skill-installation-cht-setup-claude-skills/21-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify npm pack contents and built binary</name>
  <files></files>
  <read_first>
    - package.json (verify files field is ["dist/", ".claude/skills/"])
    - dist/cht.js (verify it exists and has shebang)
  </read_first>
  <action>
1. Run the full test suite to confirm everything is green:
   ```bash
   npx vitest run --reporter=verbose
   ```

2. Rebuild to ensure dist/ is current:
   ```bash
   npm run build
   ```

3. Verify npm pack contents include the required files:
   ```bash
   npm pack --dry-run 2>&1
   ```
   Expected: output must contain:
   - `dist/cht.js` (the built binary)
   - At least 13 lines matching `.claude/skills/cht-*/SKILL.md` (one per skill)
   - Total file count should be ~15 files (1 dist + 13 skills + package.json)
   - No `src/` directory files (TypeScript source should NOT be shipped)
   - No `bin/cht.ts` (source entrypoint should NOT be shipped)
   - No `tests/` directory files

4. Verify the built binary can execute basic commands:
   ```bash
   node dist/cht.js doctor 2>&1
   ```
   Expected: JSON output with `"checks"` array

5. Check that `dist/cht.js` does not contain any `--experimental-strip-types` references:
   ```bash
   grep "experimental-strip-types" dist/cht.js
   ```
   Expected: no output (exit code 1)
  </action>
  <verify>
    <automated>cd /Users/vkosoukhov/Documents/src/chat && npx vitest run && npm run build && npm pack --dry-run 2>&1 | grep -c "SKILL.md" | xargs test 13 -le && echo "PACK OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits with code 0 (all tests pass)
    - `npm pack --dry-run` output contains `dist/cht.js`
    - `npm pack --dry-run` output contains at least 13 SKILL.md files
    - `npm pack --dry-run` output does NOT contain any `src/` files
    - `npm pack --dry-run` output does NOT contain `bin/cht.ts`
    - `node dist/cht.js doctor` outputs valid JSON with `checks` key
    - `dist/cht.js` does not contain string `experimental-strip-types`
  </acceptance_criteria>
  <done>npm pack contents verified, built binary works, full test suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of end-to-end distribution flow</name>
  <files></files>
  <action>
Human verifies the complete build-to-install pipeline works end-to-end.
  </action>
  <what-built>Complete build pipeline and distribution toolkit: tsup bundles cht binary, `cht setup` copies 13 skills + registers hooks, `cht doctor` validates installation, `cht migrate` cleans old format. All tests pass, npm pack includes correct files.</what-built>
  <how-to-verify>
1. In a terminal, navigate to the project: `cd /Users/vkosoukhov/Documents/src/chat`
2. Build and test: `npm run build && npx vitest run`
3. Test the built binary: `node dist/cht.js doctor`
   - Verify JSON output with checks for skills, hooks, CLI, storage
4. Test setup locally: `node dist/cht.js setup`
   - Verify output shows skills_copied array with 13 skills
   - Check `ls ~/.claude/skills/cht-new/SKILL.md` exists
5. Test idempotency: `node dist/cht.js setup` (run again)
   - Verify no errors, hooks_skipped should show 4 events
6. Test migrate: `node dist/cht.js migrate`
   - Verify JSON output (likely warns "nothing to migrate" if no old format exists)
7. Verify npm pack: `npm pack --dry-run`
   - Confirm dist/cht.js present, 13 SKILL.md files present, no src/ files
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint -- no automated check"</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms `cht setup` copies 13 skills to ~/.claude/skills/
    - Human confirms `cht doctor` shows check results
    - Human confirms `cht setup` second run is idempotent
    - Human confirms `npm pack --dry-run` shows correct files
  </acceptance_criteria>
  <done>Human approved the end-to-end distribution flow</done>
  <resume-signal>Type "approved" if everything works, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm pack -> npm registry | Package contents that will be published |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Information Disclosure | npm pack contents | mitigate | Verified via `npm pack --dry-run` that only dist/ and .claude/skills/ are included. No source code, tests, .env, or credentials shipped. |
</threat_model>

<verification>
1. Full test suite green
2. npm pack contains exactly the expected files
3. Built binary executes commands correctly
4. Human verifies end-to-end flow
</verification>

<success_criteria>
- All automated tests pass
- npm pack tarball is correct (dist/ + skills only)
- Human confirms setup/doctor/migrate work as expected
- Phase is ready for Phase 20 (documentation + publish)
</success_criteria>

<output>
After completion, create `.planning/phases/21-global-skill-installation-cht-setup-claude-skills/21-03-SUMMARY.md`
</output>
